<h1>Editing Book</h1>
<%= form_for(@book) do |f| %>
	Title
	<%= f.text_field :title %>
	Opinion
	<%= f.text_field :body %>

	<%= f.submit "変更を保存" %><br>

	<%= link_to "show", book_path(@book.id) %>
	<%= link_to "back", books_path %>
<% end %>