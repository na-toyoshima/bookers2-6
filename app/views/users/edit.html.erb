<% if @user.errors.any? %>
        <%= @user.errors.count %>Introduction is too long (maximum is 50 characters)
        <% @user.errors.full_messages.each do |message| %>
            <%= message %>
        <% end %>
        <% end %>

<h1>User info</h1>
ユーザ情報の編集・更新
Name,Image,Introductionを記載して更新する

<%= form_for(@user) do |f| %>
	Name
	<%= f.text_field :name %>
	Introduction
	<%= f.text_field :introduction %>

	<%= f.submit "変更を保存" %>
<% end %>