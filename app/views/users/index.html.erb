<br><br>
<div class = "container">
	<div class = "row">
		<div class = "col-xs-3">
			<%= render 'users/usercommon', user: @user %>
			<%= render 'books/bookcommon', book: @book %>
		</div>

		<div class = "col-xs-9">
			<h1>Users</h1>


			<table class="table table-striped">
				<thead>
					<tr>
					<th>image</th>
					<th>name</th>
					<th></th>
					</tr>
				</thead>

				<tbody>
					<tr>
						<% @users.each do |user| %>

						<td><%= user.name %>(imageに差し替え)</td>
						<td><%= user.name %></td>
						<td><%= link_to "Show", user_path(user.id) %></td>
					</tr>
				</tbody>
				<% end %>
			</table>
		</div>
	</div>
</div>